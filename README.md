# FastAPI Интернет-магазин

Веб-приложение интернет-магазина на FastAPI с поддержкой управления товарами, категориями, пользователями и отзывами.

## 🚀 Функциональность

### Пользователи и аутентификация
- Регистрация и авторизация пользователей
- JWT-токены (access и refresh)
- Роли: `buyer`, `seller`, `admin`
- Защита эндпоинтов по ролям

### Категории товаров
- Создание иерархических категорий
- Просмотр активных категорий
- Управление категориями (CRUD)

### Товары
- Создание товаров (только для `seller`)
- Просмотр товаров и фильтрация по категориям
- Обновление и удаление своих товаров
- Автоматический расчет рейтинга на основе отзывов

### Отзывы
- Просмотр всех отзывов и отзывов по товару
- Создание отзывов (только для `buyer`)
- Мягкое удаление отзывов (только для `admin`)
- Автоматический пересчет рейтинга товара при добавлении/удалении отзыва

## 📋 Требования

- Python 3.12+
- PostgreSQL 12+
- pip (менеджер пакетов Python)

## 🛠 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone git@github.com:Kvezac/fastapi_encommerce.git
cd fastapi_encommerce
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте файл `.env-example` в `.env`:

```bash
cp .env-example .env
```

Отредактируйте `.env` и укажите необходимые значения (см. описание в `.env-example`).

### 5. Применение миграций (создание таблиц)

**Примечание:** Alembic уже инициализирован в проекте, файлы конфигурации (`alembic.ini` и `app/migrations/env.py`) настроены для работы с PostgreSQL.

Примените миграции Alembic для создания всех таблиц в базе данных:

```bash
alembic upgrade head
```

Эта команда создаст все необходимые таблицы: `users`, `categories`, `products`, `reviews` и установит все связи между ними.

### 6. Запуск приложения

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000`

### 7. Документация API

После запуска приложения доступна интерактивная документация:
- Swagger UI: [docs](http://127.0.0.1:8000/docs)
- ReDoc: [redoc](http://127.0.0.1:8000/redoc)

## 🔐 API Эндпоинты

### Пользователи (`/users`)
- `POST /users/register` - Регистрация нового пользователя
- `POST /users/token` - Получение JWT токенов
- `POST /users/refresh` - Обновление access токена
- `GET /users/me` - Информация о текущем пользователе

### Категории (`/categories`)
- `GET /categories/` - Список всех активных категорий
- `POST /categories/` - Создание категории (только `seller`)
- `GET /categories/{category_id}` - Детали категории
- `PUT /categories/{category_id}` - Обновление категории (только `seller`)
- `DELETE /categories/{category_id}` - Удаление категории (только `seller`)

### Товары (`/products`)
- `GET /products/` - Список всех активных товаров
- `POST /products/` - Создание товара (только `seller`)
- `GET /products/{product_id}` - Детали товара
- `GET /products/{product_id}/reviews/` - Отзывы о товаре
- `GET /products/category/{category_id}` - Товары по категории
- `PUT /products/{product_id}` - Обновление товара (только `seller`, свои товары)
- `DELETE /products/{product_id}` - Удаление товара (только `seller`, свои товары)

### Отзывы (`/reviews`)
- `GET /reviews/` - Список всех активных отзывов
- `POST /reviews/` - Создание отзыва (только `buyer`)
- `DELETE /reviews/{review_id}` - Удаление отзыва (только `admin`)

## 🔑 Роли пользователей

- **buyer** - Покупатель, может создавать отзывы
- **seller** - Продавец, может управлять товарами и категориями
- **admin** - Администратор, может удалять отзывы

## 🗄 База данных

Проект использует PostgreSQL с асинхронным драйвером `asyncpg`. Все операции с БД выполняются асинхронно через SQLAlchemy 2.0.

### Основные таблицы:
- `users` - Пользователи системы
- `categories` - Категории товаров
- `products` - Товары
- `reviews` - Отзывы о товарах

## 🧪 Тестирование

Для тестирования API используйте Swagger UI (`/docs`), где можно:
- Просмотреть все доступные эндпоинты
- Отправить тестовые запросы
- Авторизоваться и получить токен
- Протестировать все функции системы

## 📝 Миграции

**Важно:** Alembic уже инициализирован и настроен в проекте. Файлы конфигурации (`alembic.ini` и `app/migrations/env.py`) уже отредактированы для работы с асинхронным PostgreSQL.

### Создание новой миграции

После изменения моделей SQLAlchemy создайте новую миграцию:

```bash
alembic revision --autogenerate -m "описание изменений"
```

### Применение миграций

```bash
alembic upgrade head
```

### Откат миграции

```bash
alembic downgrade -1
```

## 🔒 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены с ограниченным временем жизни
- Проверка ролей для доступа к эндпоинтам
- Валидация данных через Pydantic схемы

## 📄 Лицензия

Проект создан в учебных целях.
